import Vue from 'vue';
import Component from 'vue-class-component';

@Component({
    name: 'timetable-toggle',
    template: require('./index.html'),
    // props: ["", ""]
})
export class timetableTogggle extends Vue {
  
}